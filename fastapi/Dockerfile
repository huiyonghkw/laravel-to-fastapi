# 试读档 · 最小镜像（无 Alembic / 全量 EP）
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app ./app
COPY pytest.ini .
COPY tests ./tests
ENV HOST=0.0.0.0 PORT=8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
